---
- name: Create users
  block:
    - include_tasks: users.yml
  run_once: true
  tags:
    - ipausers

- name: Create groups
  block:
    - include_tasks: groups.yml
  run_once: true
  tags:
    - ipagroups

- name: Define policies
  block:
    - include_tasks: policies.yml
  run_once: true
  tags:
    - ipapolicies
